<div class="hero-unit">
  <h2>Todos Usuarios</h2>
  <%- linkTo('Criar novo usuário', addUsuarioPath, {class: 'btn pull-right'}); %>
</div>

<div id="usuarios-list">
<% if (usuarios) { %>
<% for (var i = 0, ii = usuarios.length; i < ii; i++) { %>
  <div class="row list-item" id="usuario-<%= usuarios[i].id; %>">
    <div class="span8">
      <h3><%- linkTo(usuarios[i].id, usuarioPath(usuarios[i].id)); %></h3>
    </div>
    <div class="span4">
      <h3><i class="icon-list-alt"></i><%= usuarios[i].id; %></h3>
    </div>
  </div>
<% } %>
<% } %>
</div>
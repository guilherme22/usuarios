<div class="hero-unit">
  <form id="usuario-form" class="form-horizontal" action="/usuarios/<%= params.id %>?_method=PUT" method="POST">
    <fieldset>
      <legend>Editar este usuário</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {usuario: usuario}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Editar', {type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Remover', {type: 'submit', formaction: '/usuarios/' + params.id + '?_method=DELETE', formmethod: 'POST', class: 'btn btn-danger'}) %>
      </div>
    </fieldset>
  </form>
</div>
